name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -k -A "Mozilla/5.0" -L https://monitor-ceu-uia.streamlit.app/?keepalive=1 || true
